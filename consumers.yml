# this file defines the consumers of  the secrets of the /epv/appdev/application1/consumers
# by adding hosts to this consumer group


- !grant
  role: !group epv/appdev/application1/delegation/consumers
  member: !host client/node1
